<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.agenticsdlc</groupId>
    <artifactId>agentic-sdlc-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>url-shortener</artifactId>
  <name>URL Shortener Service</name>
  <description>
    A hexagonal URL shortener with core APIs, click analytics, and reliability features.

    This module is also the TARGET CODEBASE that the orchestrator plans, modifies, tests
    and gates changes against. Its seams are deliberately clean so that brownfield
    codebase reasoning has something real to reason about.
  </description>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!--
      Spring Boot 4 split the test slices out of spring-boot-test-autoconfigure into
      per-technology modules, and moved their packages with them:
        @AutoConfigureMockMvc  org.springframework.boot.webmvc.test.autoconfigure
        @DataJpaTest           org.springframework.boot.data.jpa.test.autoconfigure
      spring-boot-starter-test no longer pulls these in, so they are declared explicitly.
      (spring-boot-starter-test-classic would restore the old aggregate, but naming the two
      slices actually used is clearer about what this module depends on.)
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
